This folder contains micro-benchmarks and sample payloads for JSONLens.

Run the primary benchmark:

  dart run tool/benchmarks/json_bench.dart

The script will generate several payload sizes in memory and write `large.json` to the same folder for UI tests.